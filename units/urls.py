# units/urls.py
from django.urls import path, include

from . import views

urlpatterns = [
    # path('', views.ListUnit.as_view()),
    # path('<int:pk>/', views.DetailUnit.as_view()),
    # path('<int:pk>/conversations/', include('conversations.urls')),
]